import 'package:flutter/material.dart';

/// ホームタブ（今月の収支サマリ）
class HomeTab extends StatelessWidget {
  /// コンストラクタ
  const HomeTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('ホーム（収支サマリ）'));
  }
}
