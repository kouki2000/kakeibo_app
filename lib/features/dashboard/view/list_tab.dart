import 'package:flutter/material.dart';

/// 明細タブ（取引一覧）
class ListTab extends StatelessWidget {
  /// コンストラクタ
  const ListTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('明細（取引一覧）'));
  }
}
